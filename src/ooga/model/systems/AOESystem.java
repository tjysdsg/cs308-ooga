package ooga.model.systems;

public class AOESystem extends ComponentBasedSystem{
    public AOESystem(EntityManager entityManager, ComponentManager componentManager) {
        super(entityManager, componentManager);
    }

    @Override
    public void update(double deltaTime) {

    }
}
