package ooga.model.systems;

/**
 * Managing the attack.
 */
public class AttackSystem extends ComponentBasedSystem{

    public AttackSystem(EntityManager entityManager, ComponentManager componentManager) {
        super(entityManager, componentManager);
    }

    @Override
    public void update(double deltaTime) {

    }
}
